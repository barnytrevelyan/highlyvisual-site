<!-- Sticky footer bar — credentials + sister sites always visible on desktop -->
<div class="credential-bar">
  <div class="container credential-bar__inner">
    <div class="credential-bar__left">
      <span class="credential-bar__label">Licensed & registered:</span>
      <span>CAA PfCO UAS 9543</span>
      <span class="credential-bar__dot">·</span>
      <span>KFCB-registered</span>
    </div>
    <div class="credential-bar__right">
      <a href="https://hirevisual.com" target="_blank" rel="noopener noreferrer">hirevisual.com</a>
      <span class="credential-bar__dot">·</span>
      <a href="https://stock.highlyvisual.com" target="_blank" rel="noopener noreferrer">stock.highlyvisual.com</a>
    </div>
  </div>
</div>

<style>
  .credential-bar {
    position: fixed;
    top: var(--header-height);
    left: 0;
    right: 0;
    z-index: 99;
    background: var(--bg);
    border-bottom: 1px solid var(--border);
    padding: 8px var(--container-pad);
    font-size: 10px;
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    display: none;
  }
  .credential-bar__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
  }
  .credential-bar__left, .credential-bar__right {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
  .credential-bar__label {
    color: var(--text-faint);
  }
  .credential-bar a {
    color: var(--text-muted);
    text-decoration: none;
    transition: color var(--motion-normal);
  }
  .credential-bar a:hover {
    color: var(--text);
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .credential-bar__dot {
    color: var(--border-strong);
  }

  /* Disabled for now - causing layout issues */
  .credential-bar { display: none !important; }
</style>
